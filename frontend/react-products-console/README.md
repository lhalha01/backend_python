# React Products Console

UI para gestionar y probar la Products API (FastAPI).

## Requisitos
- Node.js 18+.

## Configuración
- Copia `.env.example` a `.env` y ajusta `VITE_API_BASE_URL`.

## Ejecutar
```bash
npm install
npm run dev
```

Abre `http://localhost:5173`.
