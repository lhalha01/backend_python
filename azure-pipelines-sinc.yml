trigger:
  branches:
    include:
      - '*'

pool:
  vmImage: 'ubuntu-latest'

variables:
  - group: GitHub-Secrets

steps:
  - checkout: self
    persistCredentials: true

  - script: |
      git push --prune https://$(GITHUB_PAT)@github.com/lhalha01/backend_python.git $(Build.SourceBranch):$(Build.SourceBranch)
    displayName: 'Sincronizar Código a GitHub'
  